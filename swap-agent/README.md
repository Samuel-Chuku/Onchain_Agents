### To run on a fresh machine, follow the steps:

- git clone https://github.com/Samuel-Chuku/Onchain_Agents
- cd Onchain_Agents/swap-agent
- cp .env.example .env   # fill in your keys
- npm install            # patches auto-apply via postinstall
- npm start
